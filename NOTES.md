All the files will go inside bin folder

To run any file, say variables.rs use -> cargo run -q --bin variables

cargo run -q --bin <binary_name>

tuple example in math_operations
array declaration example in sum_array
